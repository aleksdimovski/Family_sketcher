//@Description Toy benchmark that explores the use of generators.


harness void sketch(int x) {

  int s=0, y=??;
  int x0=x, y0=y;
  while (x >= 0) {  
    x = x - 1;
	while (y>=0) {
		y=y-1;
		s=s+1;
	}
  }
  assert (s>=x0+y0);
}
